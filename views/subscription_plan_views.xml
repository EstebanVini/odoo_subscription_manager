<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View -->
    <record id="subscription_plan_view_form" model="ir.ui.view">
        <field name="name">subscription.plan.form</field>
        <field name="model">subscription.plan</field>
        <field name="arch" type="xml">
            <form string="Subscription Plan">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(subscription_management.action_subscription_line_all)d"
                                type="action"
                                class="oe_stat_button"
                                icon="fa-users"
                                context="{'search_default_plan_id': active_id}">
                            <field name="subscriber_count" widget="statbutton"
                                   string="Subscribers"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived"
                            invisible="active"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g., Monthly Plan"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Billing">
                            <field name="amount"/>
                            <field name="currency_id" groups="base.group_multi_currency"/>
                            <label for="interval_count" string="Billing Every"/>
                            <div class="o_row">
                                <field name="interval_count" class="oe_inline"/>
                                <field name="interval_type" class="oe_inline"/>
                            </div>
                        </group>
                        <group string="Invoicing">
                            <field name="product_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="sequence"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" placeholder="Plan description..."/>
                        </page>
                    </notebook>
                    <field name="active" invisible="1"/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Tree View -->
    <record id="subscription_plan_view_tree" model="ir.ui.view">
        <field name="name">subscription.plan.tree</field>
        <field name="model">subscription.plan</field>
        <field name="arch" type="xml">
            <tree string="Subscription Plans">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="amount" widget="monetary"/>
                <field name="interval_count"/>
                <field name="interval_type"/>
                <field name="subscriber_count"/>
                <field name="company_id" groups="base.group_multi_company" optional="hide"/>
            </tree>
        </field>
    </record>

    <!-- Search View -->
    <record id="subscription_plan_view_search" model="ir.ui.view">
        <field name="name">subscription.plan.search</field>
        <field name="model">subscription.plan</field>
        <field name="arch" type="xml">
            <search string="Subscription Plans">
                <field name="name"/>
                <filter string="Archived" name="inactive"
                        domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Interval Type" name="group_interval"
                            context="{'group_by': 'interval_type'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_subscription_plan" model="ir.actions.act_window">
        <field name="name">Subscription Plans</field>
        <field name="res_model">subscription.plan</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first subscription plan
            </p>
            <p>
                Define recurring billing plans such as Monthly, Annual, etc.
            </p>
        </field>
    </record>

</odoo>
